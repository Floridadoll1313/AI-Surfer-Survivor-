import React, { useState, useEffect } from 'react';
import { BrowserRouter as Router, Routes, Route, useOutletContext } from 'react-router-dom';
import Layout from './components/Layout';
import Home from './pages/Home';
import MapPage from './pages/MapPage'; // Import the new MapPage file

// ... (Rest of App.tsx remains the same, but you can delete the MapPage function from it)
